[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://data/notes.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
notes = PoolStringArray( "hacer que el text input haga clip del texto", "push_id para varios campos con mismo label", "hacer que ctrl+delete borre una palabra", "funcion para dibujar contenedor", "hacer input de numeros", "pensar solucion para enums", "quitar sistema de windows", "hacer un texto multi-line", "hacer ejemplo de calculadora", "mover imgui a su propia seccion", "hacer checkbox", "hacer combobox", "hacer tabs" )
