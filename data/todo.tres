[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://data/notes.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
notes = PoolStringArray( "hacer que el text input haga clip del texto", "push_id para varios campos con mismo label", "hacer que ctrl+delete borre una palabra", "funcion para dibujar contenedor", "hacer input de numeros", "quitar sistema de windows", "pensar solucion para enums", "hacer un texto multi-line" )
